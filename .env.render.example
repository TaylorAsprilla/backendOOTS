# ====================================
# RENDER PRODUCTION ENVIRONMENT VARIABLES
# ====================================
# Configurar estas variables en el Dashboard de Render:
# Dashboard > Service > Environment

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Opción 1: Base de datos externa (Railway, PlanetScale, etc.)
DB_HOST=containers-us-west-xxx.railway.app
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=tu_password_seguro_aqui
DB_DATABASE=backend_oots_prod

# Opción 2: Si usas PostgreSQL de Render (cambiar TypeORM a postgres)
# DATABASE_URL=${{DATABASE_URL}}  # Render auto-genera esta variable

# =============================================================================
# DATABASE OPTIONS - TYPEORM
# =============================================================================
DB_SYNCHRONIZE=false  # ¡NUNCA true en producción!
DB_LOGGING=false      # Reducir logs en producción

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
NODE_ENV=production
PORT=3000  # Render usa PORT automáticamente

# URLs de la aplicación
APP_URL=https://backend-oots.onrender.com  # Cambiar por tu URL de Render
FRONTEND_URL=https://tu-frontend.onrender.com  # URL de tu frontend

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
JWT_SECRET=tu_jwt_secret_super_seguro_cambiar_en_produccion_min_32_caracteres
JWT_EXPIRES_IN=7d

# =============================================================================
# MAIL CONFIGURATION - Gmail
# =============================================================================
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu-email@gmail.com
MAIL_PASSWORD=tu_app_password_de_google  # Usar App Password de Google
MAIL_FROM=noreply@tudominio.com

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
CORS_ORIGIN=https://tu-frontend.onrender.com,https://www.tudominio.com

# =============================================================================
# SECURITY
# =============================================================================
# Rate limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# =============================================================================
# MYSQL ROOT PASSWORD (si usas Docker en Render con MySQL)
# =============================================================================
MYSQL_ROOT_PASSWORD=root_password_super_seguro_cambiar

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NO subir este archivo con valores reales a Git
# 2. Configurar cada variable manualmente en Render Dashboard
# 3. Generar JWT_SECRET con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Usar App Password de Google para MAIL_PASSWORD (no tu contraseña normal)
# 5. DB_SYNCHRONIZE debe ser FALSE en producción
# 6. Render asigna PORT automáticamente, respetarlo
